FROM node:latest
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .

ARG MONGO_URL
ARG JWT_SECRET
ARG PORT

ENV MONGO_URL=$MONGO_URL
ENV JWT_SECRET=$JWT_SECRET
ENV PORT=$PORT

EXPOSE 3001
CMD [ "npm", "start" ]
